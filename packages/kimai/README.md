# kimai

Kimai timesheet export tools (`kimai-exporter`).

Depends on the `rest` workspace package for the shared HTTP helper and the
`transferwise` workspace package for the Wise exchange-rate client — fully
independent of the `harvest-exporter` package.

See [src/kimai_exporter/README.md](src/kimai_exporter/README.md) for full usage details.
